{% extends "OCStockManagementBundle::layout.html.twig" %}


{% block title %}{{ parent() }}Page de gestion des niveaux de Stock{% endblock %}

{% block body %}

{# {% form_theme formCategories _self %}

{% block integer_widget %}
    <div class="integer_widget">
        {% set type = type|default('number') %}
        {{ block('form_widget_simple') }}
    </div>
{% endblock %} #}

{# <table style="width:100%">
    <tr>
        <th>ID</th>
        <th>ParentId</th>
        <th>Libellé</th>
        <th>Niveau de stock bas</th>
    </tr>
        {% for category in formCategories %}
            <td>{{ form_row(category) }}</td>
        {% endfor %}
    <tr>

    </tr>
</table> #}

{# <div class="well">
    {{ form_row(formCategories) }}
</div> #}

        {# {{ dump(formCategories) }} #}
    {{ form_start(formCategories) }}
        {% for category in formCategories %}

            {{ form_row(category) }}
            
            {# {{ dump(category.vars.data['id']) }} #}
            {# {{ dump(category) }} #}
        {% endfor %}
        {{ form_widget(formCategories.save, { 'label': 'Click me' }) }}
    {{ form_end(formCategories) }}

    {# {% block content %}
        {{ form_row(formCategories) }}
    {% endblock %} #}

{% endblock %}